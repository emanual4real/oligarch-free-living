{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "tsx --env-file=.env --watch  src/index.ts",
    "clean": "tsc --build --clean",
    "pre-build": "tsc --build",
    "build": "./build.sh"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.0.0",
    "@types/node": "^22.13.8",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "bundledDependencies": [
    "cors",
    "express"
  ]
}
